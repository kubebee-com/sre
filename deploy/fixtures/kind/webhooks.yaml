apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: webhook-missing-service
  labels:
    sre.kubebee.com/fixture-id: webhook-missing-service
    sre.kubebee.com/fixture: "true"
webhooks:
  - name: fixture.sre.kubebee.com
    admissionReviewVersions: ["v1"]
    sideEffects: None
    failurePolicy: Ignore
    clientConfig:
      service:
        name: webhook-service-does-not-exist
        namespace: sre-fixtures
        port: 443
    rules:
      - operations: ["CREATE"]
        apiGroups: [""]
        apiVersions: ["v1"]
        resources: ["configmaps"]
