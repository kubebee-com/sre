apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sre-agent-data
  namespace: sre
  labels:
    app.kubernetes.io/name: sre-agent
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
